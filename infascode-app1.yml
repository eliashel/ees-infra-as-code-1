---
# Copyright 2018 widdix GmbH
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
AWSTemplateFormatVersion: '2010-09-09'
Description: 'Enable environment for app1'
Mappings:
  Define:
    Global:
      CompanyId: ees
      AppId: app1
      AppVersion: 0.0.1
      ProjectId: first-web-static
      CompanyName: eliashel
      CFTemplateVersion: 0.0.1
    VPC:
      CidrBlock: '10.1.0.0/16'
Metadata:
  'AWS::CloudFormation::Interface':
    ParameterGroups:
    - Label:
        default: 'Environment Parameters'
      Parameters:
      - EnvironmentId
      - EnvironmentType
Parameters:
  EnvironmentId:
    Description: 'Ensure you set unique ID for environment. You can identify all resources from the environment through this ID.'
    Type:  Number
    MinValue: 1
    MaxValue: 999
    ConstraintDescription: 'EnvironmentId must be numeric between [1-999].'
  EnvironmentType:
    Description: 'What type of the environment do you want to deploy? Select one...'
    Type: String
    AllowedValues: [dev, test, prod]
    Default: dev
    ConstraintDescription: 'Must specify: dev, test or prod'
Resources:
  VPC:
    Type: 'AWS::EC2::VPC'
    Properties:
      CidrBlock: !FindInMap [ Define, VPC, CidrBlock ]
      EnableDnsSupport: true
      EnableDnsHostnames: true
      InstanceTenancy: default
      Tags:
      - Key: Name
        Value: !Join [ '', [ !FindInMap [ Define, Global, CompanyId ], '-', !FindInMap [ Define, Global, AppId ], '-', !Sub '${EnvironmentType}-${EnvironmentId}-vpc' ] ]
      - Key: AppId
        Value: !FindInMap [ Define, Global, AppId ]
      - Key: AppVersion
        Value: !FindInMap [ Define, Global, AppVersion ]
      - Key: ProjectId
        Value: !FindInMap [ Define, Global, ProjectId ]
      - Key: CompanyId
        Value: !FindInMap [ Define, Global, CompanyId ]
      - Key: CompanyName
        Value: !FindInMap [ Define, Global, CompanyName ]
      - Key: CFTemplateVersion
        Value: !FindInMap [ Define, Global, CFTemplateVersion ]
      - Key: EnvironmentId
        Value: !Ref EnvironmentId
      - Key: EnvironmentType
        Value: !Ref EnvironmentType
  InternetGateway:
    Type: 'AWS::EC2::InternetGateway'
    Properties:
      Tags:
      - Key: Name
        Value: !Join [ '', [ !FindInMap [ Define, Global, CompanyId ], '-', !FindInMap [ Define, Global, AppId ], '-', !Sub '${EnvironmentType}-${EnvironmentId}-vpc' ] ]
      - Key: AppId
        Value: !FindInMap [ Define, Global, AppId ]
      - Key: AppVersion
        Value: !FindInMap [ Define, Global, AppVersion ]
      - Key: ProjectId
        Value: !FindInMap [ Define, Global, ProjectId ]
      - Key: CompanyId
        Value: !FindInMap [ Define, Global, CompanyId ]
      - Key: CompanyName
        Value: !FindInMap [ Define, Global, CompanyName ]
      - Key: CFTemplateVersion
        Value: !FindInMap [ Define, Global, CFTemplateVersion ]
      - Key: EnvironmentId
        Value: !Ref EnvironmentId
      - Key: EnvironmentType
        Value: !Ref EnvironmentType
  VPCGatewayAttachment:
    Type: 'AWS::EC2::VPCGatewayAttachment'
    Properties:
      VpcId: !Ref VPC
      InternetGatewayId: !Ref InternetGateway
Outputs:
  StackName:
    Description: 'Stack name.'
    Value: !Sub '${AWS::StackName}'
  VPC:
    Description: 'VPC.'
    Value: !Ref VPC
    Export:
      Name: !Sub '${AWS::StackName}-VPC'
